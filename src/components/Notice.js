import React from 'react';

const Notice = () => {
	return (
		<nav className="hidden sm:block text-center bg-[#ffdb88]">
			Work in progress for big screens ->
		</nav>
	);
};

export default Notice;
